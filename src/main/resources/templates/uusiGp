<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Uusi GP</title>
    <link rel="stylesheet" th:href="@{/css/bootstrap.rtl.min.css}" />
    <style>
        body { padding: 2rem; }
        .form-label { font-weight: bold; margin-top: 1rem; }
    </style>
</head>
<body>
<div class="container">
    <h2>Luo uusi GP</h2>

    <!-- Virheilmoitus -->
    <div th:if="${virhe != null}" class="alert alert-danger" th:text="${virhe}"></div>

    <!-- Uuden GP:n syöttölomake -->
    <form th:action="@{/admin/gp/save}" method="post">
        <div class="mb-3">
            <label class="form-label">Järjestysnumero</label>
            <input type="number" class="form-control" name="jarjestysnumero" th:value="${seuraavaNumero}" required />
        </div>

        <div class="mb-3">
            <label class="form-label">Päivämäärä</label>
            <input type="date" class="form-control" name="pvm" required />
        </div>

        <div class="mb-3">
            <label class="form-label">Aika</label>
            <input type="text" class="form-control" name="aika" placeholder="esim. 18:30" required />
        </div>

        <div class="mb-3">
            <label class="form-label">Keilahalli</label>
            <select class="form-select" name="keilahalliId" required>
                <option th:each="halli : ${keilahallit}" th:value="${halli.id}" th:text="${halli.nimi}"></option>
            </select>
        </div>

        <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" name="kultainen" id="kultainenGp">
            <label class="form-check-label" for="kultainenGp">
                Kultainen GP
            </label>
        </div>

        <div th:if="${kausiNimi != null}" class="mb-3">
            <label class="form-label">Kausi</label>
            <input type="text" class="form-control" name="kausi" th:value="${kausiNimi}" readonly />
        </div>

        <button type="submit" class="btn btn-success">Tallenna GP</button>
    </form>

    <div class="mt-4">
        <a th:href="@{/admin/gpLista}" class="btn btn-secondary">Takaisin listaan</a>
    </div>
</div>
</body>
</html>
